info:
  name: Update an user
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3333/api/users/140
  body:
    type: json
    data: |-
      {
        "name": "",
        "email": "spider@marvel.com",
        "password": "pwd123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
